apply plugin: 'java'
apply plugin: 'maven-publish'

group = 'com.googlecode.json-simple'
version = '1.1.1'

description = "JSON.simple - A simple Java toolkit for JSON"

sourceCompatibility = 1.5
targetCompatibility = 1.5

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}

manifest {
    attributes(
            "Bundle-Name": "JSON.simple",
            "Bundle-SymbolicName": "com.googlecode.json-simple.json-simple",
            "Bundle-Version": "${project.version}",
            "Bundle-License": "http://www.apache.org/licenses/LICENSE-2.0.txt",
            "Bundle-Description": "A simple Java toolkit for JSON",
            "Export-Package": "org.json.simple, org.json.simple.parser"
    )
}

task sourcesJar(type: Jar, dependsOn: classes) {
    classifier = 'sources'
    from sourceSets.main.allSource
}

task javadocJar(type: Jar, dependsOn: javadoc) {
    classifier = 'javadoc'
    from javadoc.destinationDir
}

artifacts {
    javadocJar
    sourcesJar
}

publishing {
    def login = file("credentials").text.split(":", 2)
    if (login == null || login.length != 2) return
    repositories {
        maven {
            url 'http://heisluft.bplaced.net/upload/'
            credentials(HttpHeaderCredentials) {
                name = login[0]
                value = login[1]
            }
            authentication {
                viaHeader(HttpHeaderAuthentication)
            }
        }
    }
    publications {
        maven(MavenPublication) {
            pom {
                licenses {
                    license {
                        name = 'The Apache Software License, Version 2.0'
                        url = 'http://www.apache.org/licenses/LICENSE-2.0.txt'
                    }
                }
                developers {
                    developer {
                        id = 'Yidong'
                        name = 'Yidong Fang'
                        email = 'fangyidong@yahoo.com.cn'
                    }
                    developer {
                        id = 'heisluft'
                        name = 'heisluft'
                        email = 'heisluftlp@gmail.com'
                    }
                }
                scm {
                    connection = 'scm:git:https://github.com/heisluft/json-simple'
                    developerConnection = 'scm:git:https://github.com/heisluft/json-simple'
                    url = 'https://github.com/heisluft/json-simple'
                }
            }
        }
    }
}

publish.dependsOn test

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
